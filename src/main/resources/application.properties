spring.application.name=kafka-consumer
server.port=8089

# Kafka config
spring.kafka.bootstrap-servers=localhost:29092,localhost:39092
spring.kafka.consumer.group-id=group_id
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# Variables
kafka.bootstrap.topic=alertas
kafka.consumer.group-id=group_id
kafka.bootstrap.servers=localhost:29092,localhost:39092

api.measurement=${API_MEASUREMENT_URL:http://localhost:8085}